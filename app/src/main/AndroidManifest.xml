<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="il.ac.huji.todolistmanager" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".TodoListManagerActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddNewTodoItemActivity"
            android:label="@string/add_new_todo_item"
            android:theme="@style/Theme.AlertDialogPro.Material.Light" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="il.ac.huji.todolistmanager.TodoListManagerActivity" />
        </activity>
    </application>

    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>

</manifest>
